<%= form_for(film) do |f| %>
  <% if film.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(film.errors.count, "error") %> prohibited this film from being saved:</h2>

      <ul>
      <% film.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titre %>
    <%= f.text_field :titre %>
  </div>

  <div class="field">
    <%= f.label :date_sortie %>
    <%= f.datetime_select :date_sortie %>
  </div>

  <div class="field">
    <%= f.label :synopsis %>
    <%= f.text_field :synopsis %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
